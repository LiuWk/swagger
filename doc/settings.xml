<?xml version="1.0" encoding="UTF-8"?>

<settings xmlns="http://maven.apache.org/SETTINGS/1.0.0"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/SETTINGS/1.0.0 http://maven.apache.org/xsd/settings-1.0.0.xsd">

    <localRepository>D:/maven/local</localRepository>

    <pluginGroups>
        <pluginGroup>org.mortbay.jetty</pluginGroup>
    </pluginGroups>

    <proxies></proxies>

    <servers>
        <server>
            <id>nexusServer</id>
            <username>deployment</username>
            <password>dep.BAIHE</password>
        </server>
    </servers>

    <mirrors>
        <!--阿里云镜像-->
        <mirror>  
                 
            <id>nexus-aliyun</id>
            <name>nexus-aliyun</name>
                  <url>http://maven.aliyun.com/nexus/content/groups/public/</url>  
                  <mirrorOf>central</mirrorOf>          
                
        </mirror>
        <mirror>
            <!--This sends everything else to /public -->
            <id>nexusServer</id>
            <mirrorOf>*</mirrorOf>
            <url>http://maven.baihe.com/nexus/content/groups/development</url>
        </mirror>

    </mirrors>

    <profiles>
        <profile>
            <id>nexusServer</id>
            <!--Enable snapshots for the built in central repo to direct -->
            <!--all requests to nexus via the mirror -->
            <repositories>
                <repository>
                    <id>central</id>
                    <url>http://maven.baihe.com/nexus/content/groups/development</url>
                    <releases>
                        <enabled>true</enabled>
                    </releases>
                    <snapshots>
                        <enabled>true</enabled>
                    </snapshots>
                </repository>
            </pluginRepositories>

        </profile>

        <profile>
            <!-- id必须唯一 -->
            <id>nexus-aliyun</id>
            <repositories>
                <repository>
                    <!-- id必须唯一 -->
                    <id>aliyun-repository</id>
                    <!-- 见名知意即可 -->
                    <name>aliyun repository</name>
                    <!-- 仓库的url地址 -->
                    <url>http://maven.aliyun.com/nexus/content/groups/public/</url>
                </repository>
            </repositories>
            <pluginRepositories>
                <pluginRepository>
                    <id>central</id>
                    <url>http://maven.baihe.com/nexus/content/groups/development</url>
                    <releases>
                        <enabled>true</enabled>
                    </releases>
                    <snapshots>
                        <enabled>true</enabled>
                    </snapshots>
                </pluginRepository>
            </repositories>
        </profile>

    </profiles>

    <activeProfiles>
        <activeProfile>nexusServer</activeProfile>
    </activeProfiles>

</settings>
